import {
  registerFetch,
  loginFetch,
  logoutFetch,
  authenticateFetch,
} from "./authentication";
import { gamesFetch, newGameFetch } from "./game";
export {
  registerFetch,
  loginFetch,
  logoutFetch,
  authenticateFetch,
  gamesFetch,
  newGameFetch,
};
