import {
  registerFetch,
  loginFetch,
  logoutFetch,
  refreshFetch,
} from "./authentication";
import {
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
  roundsByGameIdFetch,
  newRoundByGameIdFetch
} from "./game";
export {
  registerFetch,
  loginFetch,
  logoutFetch,
  refreshFetch,
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
  roundsByGameIdFetch,
  newRoundByGameIdFetch
};
