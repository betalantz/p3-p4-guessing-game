import {
  registerFetch,
  loginFetch,
  logoutFetch,
  authenticateFetch,
} from "./authentication";
import {
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
  roundsByGameIdFetch,
  newRoundByGameIdFetch
} from "./game";
export {
  registerFetch,
  loginFetch,
  logoutFetch,
  authenticateFetch,
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
  roundsByGameIdFetch,
  newRoundByGameIdFetch
};
