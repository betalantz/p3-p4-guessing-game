import {
  registerFetch,
  loginFetch,
  logoutFetch,
  authenticateFetch,
} from "./authentication";
import {
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
} from "./game";
export {
  registerFetch,
  loginFetch,
  logoutFetch,
  authenticateFetch,
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
};
