import {
  registerFetch,
  loginFetch,
  logoutFetch,
  refreshFetch,
} from "./authentication";
import {
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
  roundsByGameIdFetch,
} from "./game";
export {
  registerFetch,
  loginFetch,
  logoutFetch,
  refreshFetch,
  gamesFetch,
  postGamesFetch,
  patchGamesFetch,
  gamesByIdFetch,
  deleteGamesByIdFetch,
  roundsByGameIdFetch,
};
